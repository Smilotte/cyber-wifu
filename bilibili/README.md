# Bilibili API Client

## Prepare
- Install packages
  ```powershell
  pip install requests
  pip install websockets
  ```

## Run
- ```
  python main.py ${ROOMID} ${KEY} ${SECRET} [--host ${HOST}]
  ```